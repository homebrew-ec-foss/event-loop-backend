# Event Loop

For nginx, place eventloop.hegde.live at /etc/nginx/sites-available.

Create symlink
ln -s /etc/nginx/sites-available/eventloop.hegde.live /etc/nginx/sites-enabled/

remove symlink for default site
rm /etc/nginx/sites-enabled/default

restart nginx
